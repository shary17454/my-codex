# النشر

- `docker-compose up --build` للتطوير.
- `docker-compose -f docker-compose.production.yml up --build` للإنتاج.

المتطلبات:
- PostgreSQL
- Redis
- مفاتيح البيئة في .env

ملاحظات:
- الواجهات تعمل على 3001 و3002 حسب الإعداد الحالي.
